# Tibox

La intranet TIBOX HUB sigue temporalmente en la raíz del repositorio para no romper el flujo de build actual. En una segunda etapa se moverá a `clientes/tibox/tibox-hub/` manteniendo su historial y CI.
